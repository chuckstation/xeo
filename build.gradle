// Top-level build file — configuration for all sub-projects/modules.
plugins {
    // AGP 8.5+ is required for compileSdk 35 (Android 15).
    id 'com.android.application' version '8.5.2' apply false
}
