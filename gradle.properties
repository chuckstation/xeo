# JVM heap for the Gradle daemon — C++ compilation is memory-hungry
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8

# AndroidX
android.useAndroidX=true

# Gradle build performance
org.gradle.caching=true
org.gradle.parallel=true
org.gradle.daemon=true

# Kotlin (not used in this project, but suppresses a noisy AGP warning)
kotlin.stdlib.default.dependency=false

# Xeo project metadata — consumed by build scripts and CI
xeo.version=0.5.3
xeo.versionCode=6
xeo.applicationId=org.adars.xeo
