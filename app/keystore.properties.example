# Copy this file to app/keystore.properties and fill in your keystore details.
# The release build will pick it up automatically — see app/build.gradle.
# NEVER commit the real keystore.properties file. It is git-ignored by default.

storeFile=../release.keystore
storePassword=change-me
keyAlias=xeo-release
keyPassword=change-me
